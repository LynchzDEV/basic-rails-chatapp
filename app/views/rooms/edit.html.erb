<% content_for :title, "Editing room" %>

<div class="container">
  <h1>Editing room</h1>

  <%= render "form", room: @room %>

  <div class="nav-links">
    <%= link_to "Show this room", @room, class: "button" %>
    <%= link_to "Back to rooms", rooms_path, class: "button secondary" %>
  </div>
</div>
